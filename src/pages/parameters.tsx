import React from "react";
import { Container, SectionTitle } from "../../styles/pages/parameters";

const parameters = () => {
  return (
    <Container>
      <SectionTitle>
        <h1>Parametrização</h1>
        <p>Selecione a semana que deseja gerar</p>
      </SectionTitle>
      <span>Parametrização</span>
    </Container>
  );
};

export default parameters;
